Write a commit message for the staged changes below.

<format>
- Subject under 50 chars, blank line, then optional body
- Output only the commit message, no quotes or code blocks
</format>

<style>
- Imperative mood: "Add feature" not "Added feature"
- Prefer one-line commit messages, unless the commit has a unique need for
  additional explanation (major feature, security fix explanation, etc.)
- Subject:
  - Conventional Commits format
  - Describe the change, not the intent or benefit
- Body:
  - Markdown bullets
  - Key elements of change only, omit inessential details
  - Do not provide a blow-by-blow summary
</style>

<diffstat>
{{ git_diff_stat }}
</diffstat>

<diff>
{{ git_diff }}
</diff>

<context>
Branch: {{ branch }}
{% if recent_commits %}<recent_commits>
{% for commit in recent_commits %}- {{ commit }}
{% endfor %}</recent_commits>{% endif %}
</context>
